---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - sudo pip install ansible

script:
  - "ansible-playbook zend-server.yml --syntax-check"
  - "ansible-playbook zend-server.yml"
